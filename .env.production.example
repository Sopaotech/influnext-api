# ==========================================
# INFLUNEXT - PRODUCTION ENVIRONMENT VARIABLES (OFFICIAL DOMAIN)
# ==========================================

# ─── Configurações Básicas ───────────────────────────────────────────────
NODE_ENV=production
PORT=4000

# ─── Domínios e CORS (Multi-Domínio) ─────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://influnext.com.br
NEXT_PUBLIC_API_URL=https://api.influnext.com.br/v1
ALLOWED_ORIGINS=https://influnext.com.br,https://www.influnext.com.br
FRONTEND_URL=https://influnext.com.br

# Domínio base para o Cookie JWT (Essencial para Auth funcionar em subdomínios)
NEXT_PUBLIC_COOKIE_DOMAIN=.influnext.com.br

# ─── Banco de Dados (PostgreSQL) ─────────────────────────────────────────
# URL do banco de produção (Railway, Supabase, Neon)
DATABASE_URL="postgresql://user:password@host:port/dbname?schema=public"

# ─── Segurança (JWT & Escrow) ────────────────────────────────────────────
JWT_SECRET="GERAR_NO_TERMINAL_COM_CRYPTO_RANDOMBYTES"

# Chaves dedicadas para criptografia de tokens sociais (AES-256-GCM, 32 bytes hex)
SOCIAL_TOKEN_ACTIVE_KEY_ID="v1"
SOCIAL_TOKEN_KEY_V1="GERAR_64_CARACTERES_HEXADECIMAIS"

# ─── Redis (BullMQ / Filas) ──────────────────────────────────────────────
REDIS_URL="rediss://default:password@host:port"

# ─── APIs Externas (IA & Uploads) ────────────────────────────────────────
GEMINI_API_KEY="sua_chave_gemini_1.5_flash"
CLOUDINARY_URL="cloudinary://api_key:api_secret@cloud_name"

# ─── Gateway de Pagamento (Stripe) ──────────────────────────────────────────
STRIPE_SECRET_KEY="sk_live_sua_chave_secreta_stripe"
STRIPE_WEBHOOK_SECRET="whsec_seu_webhook_secret_stripe"

# Assinatura secreta gerada em Mercado Pago > Suas integrações > Webhooks
MERCADOPAGO_WEBHOOK_SECRET="assinatura_secreta_do_webhook_mercado_pago"

# IDs dos Preços dos Planos (Stripe Price IDs)
STRIPE_PRICE_PRO="price_id_do_plano_pro"
STRIPE_PRICE_ENTERPRISE="price_id_do_plano_enterprise"

# ─── Autenticação Instagram (Professional/Creator Login) ─────────────────────
# Utilizado pelo fluxo unificado do Instagram API com Instagram Login.
# Requer um app configurado com o produto "Instagram" (não mais Basic Display).
INSTAGRAM_CLIENT_ID="seu_instagram_app_client_id"
INSTAGRAM_CLIENT_SECRET="seu_instagram_app_client_secret"
